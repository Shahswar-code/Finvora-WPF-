namespace Finvora.Models
{
    /// <summary>
    /// Every value here must have a matching Resources/Themes/{Name}Theme.xaml
    /// file that defines the same resource keys as DarkNavyTheme.xaml.
    /// </summary>
    public enum AppTheme
    {
        DarkNavy,
        Light,
        CyberDark,
        MidnightPurple,
        EmeraldForest
    }
} 