<UserControl x:Class="Finvora.Views.ComingSoonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="360">
            <TextBlock Text="&#xE81C;" FontFamily="Segoe MDL2 Assets" FontSize="40"
                       Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Center" Margin="0,0,0,16"/>
            <TextBlock Text="{Binding Title}" Style="{StaticResource TextHeading}" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Message}" Style="{StaticResource TextCaption}"
                       TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl> 