<UserControl x:Class="Finvora.Views.StatCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Border Background="{DynamicResource SurfaceCardBrush}" CornerRadius="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{Binding AccentBrush, ElementName=Root}"
                    CornerRadius="10,0,0,10"/>

            <Grid Grid.Column="1" Margin="16,14">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Title, ElementName=Root}"
                           Style="{StaticResource TextCaption}" VerticalAlignment="Center"/>

                <Border Grid.Row="0" Grid.Column="1" Background="{Binding AccentBrush, ElementName=Root}"
                        Opacity="0.18" CornerRadius="5" Padding="7,3">
                    <TextBlock Text="{Binding Badge, ElementName=Root}"
                              Foreground="{Binding AccentBrush, ElementName=Root}"
                              FontFamily="Segoe MDL2 Assets" FontSize="13"/>
                </Border>

                <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                           Text="{Binding Value, ElementName=Root}"
                           Style="{StaticResource TextMetric}" Margin="0,8,0,0"/>
            </Grid>
        </Grid>
    </Border>
</UserControl> 